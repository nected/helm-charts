apiVersion: v1
kind: Service
metadata:
  name: {{ include "nalanda.fullname" . }}
  labels:
    {{- include "nalanda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nalanda.selectorLabels" . | nindent 4 }}
