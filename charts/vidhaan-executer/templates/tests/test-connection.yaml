apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vidhaan-executer.fullname" . }}-test-connection"
  labels:
    {{- include "vidhaan-executer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vidhaan-executer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
