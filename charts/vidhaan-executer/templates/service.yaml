apiVersion: v1
kind: Service
metadata:
  name: {{ include "vidhaan-executer.fullname" . }}
  labels:
    {{- include "vidhaan-executer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vidhaan-executer.selectorLabels" . | nindent 4 }}
