apiVersion: v1
kind: Service
metadata:
  name: {{ include "nected-editor.fullname" . }}
  labels:
    {{- include "nected-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nected-editor.selectorLabels" . | nindent 4 }}
