{{- include "inherited.defaultConfig" . -}}
{{- $staticCmap := include "nected-editor.staticConfig" . }} 
{{- $defaultConfig := include "nected-editor.defaultConfig" . }} 


{{- $finalCMap := include "common.tplvalues.merge" ( dict "values" ( list $defaultConfig  $staticCmap ) "context" . ) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nected-editor.name" . }}
  labels:
    {{- include "nected-editor.labels" . | nindent 4 }}
data:
    envFile: |
    {{- include "common.tplvalues.render" ( dict "value" $finalCMap "context" .) | nindent 4 }}