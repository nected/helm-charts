apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vidhaan-router.fullname" . }}-test-connection"
  labels:
    {{- include "vidhaan-router.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vidhaan-router.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
