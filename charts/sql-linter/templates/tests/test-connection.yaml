apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sql-linter.fullname" . }}-test-connection"
  labels:
    {{- include "sql-linter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sql-linter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
