apiVersion: v1
kind: Service
metadata:
  name: {{ include "sql-linter.fullname" . }}
  labels:
    {{- include "sql-linter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sql-linter.selectorLabels" . | nindent 4 }}
