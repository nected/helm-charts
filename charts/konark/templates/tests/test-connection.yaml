apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konark.fullname" . }}-test-connection"
  labels:
    {{- include "konark.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konark.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
