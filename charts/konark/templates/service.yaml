apiVersion: v1
kind: Service
metadata:
  name: {{ include "konark.fullname" . }}
  labels:
    {{- include "konark.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "konark.selectorLabels" . | nindent 4 }}
